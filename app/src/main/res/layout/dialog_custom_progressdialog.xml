<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/progress_dialog_background"
    android:clipChildren="false"
    android:clipToPadding="false"
    >

    <RelativeLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/dialog_custom_progressdialog_linear"
        android:clipChildren="false"
        android:clipToPadding="false"
        >
        <Button
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:id="@+id/dialog_custom_progressdialog_close_button"
            android:background="@drawable/circle_button"
            android:layout_gravity="right|end"
            android:layout_marginTop="-8dp"
            android:layout_marginRight="-8dp"
            android:layout_alignParentRight="true"
            />


        <ProgressBar
            style="?android:attr/progressBarStyleLarge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/progressBar"
            android:layout_gravity="center"
            android:layout_centerInParent="true"
            android:scaleX="0.5"
            android:scaleY="0.5"
            android:indeterminateTint="@android:color/white"
            android:layout_marginTop="-8dp"/>
    </RelativeLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/dialog_custom_progressdialog_msg"
        android:layout_weight="1"
        android:text="@string/dialog_custom_progressdialog_msg"
        android:textColor="@android:color/white"
        android:layout_below="@id/dialog_custom_progressdialog_linear"
        android:textSize="20sp"
        android:layout_marginBottom="10dp"
        android:gravity="center"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>